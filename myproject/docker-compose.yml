version: "3"
services:
    web:
        build: . 
        image: myrepo/myproject:latest
        command: python manage.py runserver 0.0.0.0:8000
        restart: always
        ports:
            - "8000:8000"
