version: '3'

services:
  web:
    build: .
    environment:
        - PYTHONPATH=/code
    command: python manage.py runserver 0.0.0.0:8000 && cron
    volumes:
      - .:/code
    ports:
      - "8000:8000"
