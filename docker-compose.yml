version: '3'

services:
  web:
    build: .
    environment:
        - PYTHONPATH=/code
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    entrypoint:
      - /code/docker/entry_point.sh
